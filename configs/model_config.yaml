model:
  provider: deepseek
  name: deepseek-chat
  temperature: 0.2
  max_tokens: 1200
  timeout_seconds: 60

runtime:
  api_key_env: DEEPSEEK_API_KEY
  base_url_env: DEEPSEEK_BASE_URL
  enable_streaming: false
  retry:
    max_attempts: 3
    backoff_seconds: 2
